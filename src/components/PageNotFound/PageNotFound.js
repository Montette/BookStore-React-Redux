import React from 'react';
import './PageNotFound.scss';


const PageNotFound = (props) => {
    
    return (
      <div><h2 className={'pageNotFound__title'}>Page not found </h2> 
      </div>
    );
}

export default PageNotFound;